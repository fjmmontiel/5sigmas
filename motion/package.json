{
  "name": "5sigmas-motion",
  "version": "4.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic editorial motion engine for 5sigmas. Shared timeline, semantic mechanisms, locked series identities and series-level visual-diversity gates.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "gate": "node scripts/check_release_gate.mjs",
    "gate:visual": "node scripts/check_visual_variety.mjs",
    "gate:release": "node scripts/check_release_gate.mjs --require-ready"
  },
  "engines": {
    "node": ">=18"
  }
}
